import React from "react";
const userContext = React.createContext()


export default userContext;

// this is how we create context object using react.createcontext() method
// this is main obj from which provider and consumer are created
//this is first step to create context api